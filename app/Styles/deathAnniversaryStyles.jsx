const {StyleSheet} = require('react-native');

const deathAnniversaryStyles = StyleSheet.create({
  main: {
    paddingVertical: 20,
    paddingHorizontal: 15,
    backgroundColor: '#f0f0f0',
  },
});

export default deathAnniversaryStyles;
